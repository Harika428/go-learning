package main
import "fmt" 
func main() {
 
var elements := make(map[string]string)
    elements["H"] = "harika"
    elements["A"] = "Apple"
	elements["B"] = "Ball"
	elemants["c"] = "cat"
    elements["d"] = "dog" 
	fmt.Println(elements["d"])
 }
  
  
  
